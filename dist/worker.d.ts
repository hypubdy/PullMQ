import { EventEmitter } from 'events';
import type Redis from 'ioredis';
import type { WorkerOptions, Processor } from './types';
export declare class Worker<DataType = unknown, ResultType = unknown, NameType extends string = string> extends EventEmitter {
    private processor?;
    readonly name: string;
    readonly opts: WorkerOptions;
    readonly id: string;
    private client;
    private blockingClient;
    private _running;
    private _closing;
    private _paused;
    private activeCount;
    private activeJobs;
    private schedulerTimer;
    private delayedTimer;
    private stalledTimer;
    constructor(name: string, processor?: (Processor<DataType, ResultType, NameType> | null) | undefined, opts?: WorkerOptions);
    get keyPrefix(): string;
    get concurrency(): number;
    set concurrency(val: number);
    isRunning(): boolean;
    isPaused(): boolean;
    pause(doNotWaitActive?: boolean): Promise<void>;
    resume(): void;
    waitUntilReady(): Promise<Redis>;
    cancelJob(jobId: string, reason?: string): boolean;
    cancelAllJobs(reason?: string): void;
    run(): Promise<void>;
    private mainLoop;
    private nextFromPriority;
    private processJob;
    private lockAndActivate;
    private cleanup;
    private onCompleted;
    private onFailed;
    private calcBackoff;
    private applyRemovePolicy;
    private startScheduler;
    private scheduleGroupJob;
    private startDelayedPromoter;
    private promoteDelayedJobs;
    private startStalledChecker;
    private checkStalledJobs;
    private xadd;
    rateLimit(expireTimeMs: number): Promise<void>;
    close(force?: boolean): Promise<void>;
}
//# sourceMappingURL=worker.d.ts.map